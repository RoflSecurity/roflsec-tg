module.exports = {
  execute: async (ctx) => {
    if (!ctx.inlineQuery) return;

    // Placeholder pour les inline queries
    // On peut générer une liste de commandes en mode inline si besoin
  }
};
